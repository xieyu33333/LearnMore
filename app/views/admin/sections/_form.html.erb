<%= form_for([:admin, @section], :html => {:multipart => true}) do |f| %>
  <% if @section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% @section.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label '版块名称：' %><br />
    <%= f.text_field :name %>
  </div>

  <p>版块图片：</p>
  <input id = "picture_uploader" type="file" name="sectionLogo[]" />

  <p>添加作者：</p>
  <div class="section_user">
    <% if @section.users.any?%>
      <% @section.users.each do |user| %>
        <input type="text" name="users[]" value="<%= user.username%>"/>
      <% end%>
    <% end%>
    <input name='users[]' type="text" />
  </div>
  <div class="addmore"></div>
  <div class="plusmore"></div></br>

  <div class="field">
    <%= f.label '版块介绍：' %><br />
    <%= f.text_area :description %>
  </div>
 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
