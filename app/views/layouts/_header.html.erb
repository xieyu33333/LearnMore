<div class="navbar navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <a class="logo" href="/sections"></a>
      <div style="float:left">
        <a class="brand" href="/sections">Text-Learn</a>
        <ul class="nav">
          <li><a href="/sections/ask_for_section">申请版块</a></li>
          <% if current_user && current_user.admin? %>
            <li><a href="/admin/studyfiles">后台管理</a></li>
          <% end %>
       </ul>
      </div> 
      <div style="float:right">
        <ul class="nav">
          <% if current_user%>
            <% if current_user.message.count > 0%>
              <li><a class="message_tip_red" href=<%= "/messages"%>><%= current_user.message.count%></a></li>
            <% else%>
              <li><a class="message_tip" href=<%= "/messages"%>>0</a></li>
            <% end%>
            <li><%= link_to current_user.username, current_user %></li>
            <li><%= link_to('登出', destroy_user_session_path, :method => :delete) %> </li>
          <% else%>
            <li><%= link_to('登录', new_user_session_path)  %></li>
            <li><a href="/users/sign_up">注册</a></li>
          <% end%>
        </ul>
      </div>
    </div>
  </div>
</div>